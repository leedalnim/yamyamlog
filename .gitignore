node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log

tsconfig.tsbuildinfo
dist-single
# 루트에 임시로 만드는 일회용 스크립트만 무시한다.
# (scripts/ 아래 빌드 검사 스크립트까지 삼키지 않도록 루트 한정)
/*.mjs

# scripts/ 아래는 빌드·배포에 필요한 파일이므로 절대 무시하지 않는다.
# 예전에 '*.mjs' 규칙이 검사 스크립트를 삼켜 배포가 실패한 적이 있다.
!scripts/
!scripts/**
dist-test

# 작업 중 찍은 확인용 스크린샷 (저장소에 남기지 않는다)
/*.png
