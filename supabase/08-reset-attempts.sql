-- 참여 실패 기록 지우기.
--
-- 코드가 잘려서 서버로 가던 시기에 참여를 여러 번 시도했다면, 실패가
-- 쌓여 있을 수 있다. 한 시간에 10번 실패하면 그 뒤로는 코드가 맞아도
-- '시도가 너무 많아요' 로 막힌다 (무차별 대입 차단 장치).
--
-- 아래를 돌리면 그 기록이 지워져 바로 다시 시도할 수 있다.
-- 실패 기록일 뿐이라 지워도 잃는 것이 없다.

delete from join_attempts;

-- 확인 — 0 이 나와야 한다
select count(*) as 남은_시도기록 from join_attempts;
